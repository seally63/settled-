// Re-export request detail so navigation stays within the trades tab stack
// (so the X button on the request page returns to the trade home, not Projects)
export { default } from '../../quotes/request/[id]';
